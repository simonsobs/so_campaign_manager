name: Tests
permissions:
  contents: read
  pull-requests: write

on: [pull_request]

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - uses: astral-sh/setup-uv@v5
      - run: |
             uv sync --group dev
             uv run pytest -vvv tests/ --cov --cov-append
      - uses: coverallsapp/github-action@v2
        with:
          file: .coverage
